import LoginForm from "../components/LoginForm/LoginForm";

const Login = () => {
  return (
    <main>
      <section id="LoginForm">
        <LoginForm />
      </section>
    </main>
  );
};

export default Login;
